<style>
    .multicolumn{  text-align: center;}
     @media (max-width: 789px){
  .multicolumn img { max-width: 120px;}
     }
   .custom-slideshow  ul.slick-dots{    margin-top: -40px !important;}
   .custom-slideshow .slick-dots li button {
      width: 30px;border-radius:0;
      height: 5px;
  }
  /* Define the keyframes for the background color animation */
  @keyframes backgroundProgress {
    0% {
      background-color: #ffffff;
    }
    100% {
      background-color: #06374f;
    }
  }

  /* Apply styles to the slick-active button */
  .custom-slideshow .slick-dots .slick-active button {
    background-color: #ffffff; /* Initial background color */
    animation: backgroundProgress 6s forwards; /* Apply the animation */
  }

  /* Optional: Ensure the button's transition is smooth */
  .custom-slideshow .slick-dots button {
    transition: background-color 0.3s ease;
  }
  .custom-slideshow:not(.slick-initialized) .slidebanner:not(.slidebanner1){display:none;}

  /* CountDown */
  .custom-banner-gd {
    position: relative;
}
.end-deal-counter div span {
  color: #004E80;
  font-family: MikadoMedium;
  font-size: 44px;
  font-style: normal;
  font-weight: 700;
  line-height: 44px;
}
.end-deal-counter,.end-deal-counter div {
  color: #06374F;
  font-family: MikadoMedium;
  font-size: 18px;
  font-style: normal;
  font-weight: 400;
  line-height: normal;
}
.slide-count {
  display: flex;
  align-items: center;
  background: #ffffff;
  border-radius: 6px;
  padding: 3px;
  justify-content: center;
  border: 3.04px solid #004E80;
}
.end-deal-counter {
  display: flex !important;
  align-items: center;
  gap: 15px;
}
.end-deal-counter div {
   display: block;
   text-align: center
}
.cust-time {
  position: relative;
}
.slider-countdown {
  position: absolute;
  bottom: 27%;
  left: 14.3%;
  width: 100%;
  max-width: 320px;
  transform: translateX(-14.3%);
}

@media (min-width:1501px) and (max-width: 1700px){
  .slide-count { gap: 10px; padding: 5px }
  .end-deal-counter { gap: 15px; }
  .end-deal-counter div span { font-size: 32px; line-height: 32px; }
  .slider-countdown .slide-cus-btn { font-size: 16px; max-width: 200px; height: 35px; margin: 5px auto 0 }
  .slide-count p,.end-deal-counter,.end-deal-counter div { font-size: 14px; }
  .slider-countdown { bottom: 27%; left: 14.5%; transform: translateX(-14.5%); max-width: 260px; }
}

@media (min-width:1401px) and (max-width: 1500px){
  .slide-count { gap: 10px; padding: 5px 7}
  .end-deal-counter { gap: 12px; }
  .end-deal-counter div span { font-size: 30px; line-height: 30px; }
  .slider-countdown .slide-cus-btn { font-size: 20px; max-width: 180px; height: 35px; margin: 5px auto 0 }
  .slide-count p,.end-deal-counter,.end-deal-counter div { font-size: 12px; }
  .slider-countdown { bottom: 27%; left: 14.5%; transform: translateX(-14.5%); max-width: 240px; }
}

@media (min-width:1301px) and (max-width: 1400px){
  .slide-count { gap: 10px; padding: 5px }
  .end-deal-counter { gap: 10px; }
  .end-deal-counter div span { font-size: 26px; line-height: 20px; }
  .slider-countdown .slide-cus-btn { font-size: 16px; max-width: 180px; height: 35px; margin: 5px auto 0 }
  .slide-count p,.end-deal-counter,.end-deal-counter div { font-size: 10px; }
  .slider-countdown { bottom: 28%; left: 14.5%; transform: translateX(-14.5%); max-width: 220px; }
}
  
@media (min-width:1200px) and (max-width: 1300px){
  .slide-count { gap: 10px; padding: 6px }
  .end-deal-counter { gap: 12px; }
  .end-deal-counter div span { font-size: 24px; line-height: 16px; }
  .slider-countdown .slide-cus-btn { font-size: 16px; max-width: 160px; height: 30px; margin: 5px auto 0 }
  .slide-count p,.end-deal-counter,.end-deal-counter div { font-size: 10px; }
  .slider-countdown { bottom: 27%; left: 14.5%; transform: translateX(-14.5%); max-width: 210px; }
}

@media (min-width:1000px) and (max-width: 1199px){
  .slide-count { gap: 5px; padding: 4px }
  .end-deal-counter { gap: 10px; }
  .end-deal-counter div span { font-size: 16px; line-height: 16px; }
  .slider-countdown .slide-cus-btn { font-size: 16px; max-width: 140px; height: 28px; margin: 5px auto 0 }
  .slide-count p,.end-deal-counter,.end-deal-counter div { font-size: 10px; }
  .slider-countdown { bottom: 27%; left: 14.5%; transform: translateX(-14.5%); max-width: 180px; }
}
@media (min-width: 790px) and (max-width: 999px) {
  .slide-count { gap: 5px; padding: 0px }
  .end-deal-counter { gap: 5px; }
  .end-deal-counter div span { font-size: 14px; line-height: 16px; }
  .slider-countdown .slide-cus-btn { font-size: 12px; max-width: 140px; height: 28px; margin: 5px auto 0 }
  .slide-count p,.end-deal-counter,.end-deal-counter div { font-size: 10px; }
  .slider-countdown { bottom: 27%; left: 14%; transform: translateX(-14%); max-width: 168px; }
}
@media (min-width: 651px) and (max-width: 789px) {
  .slider-countdown { position: absolute; bottom: 58%; left: 50%; transform: translateX(-50%); width: 100%; }
}
@media (min-width: 501px) and (max-width: 650px) {
  .slider-countdown { bottom: 57%; left: 49%; transform: translateX(-49%); }
}
@media (min-width: 431px) and (max-width: 500px) {
  .slider-countdown { bottom: 55%; left: 49%; transform: translateX(-49%); }
}
@media (min-width: 421px) and (max-width: 430px) {
  .slider-countdown { bottom: 283px; left: 49%; max-width: 255px; transform: translateX(-49%); }
  .end-deal-counter div span { font-size: 28px; line-height: 30px; }
  .end-deal-counter, .end-deal-counter div { font-size: 14px; }
  .end-deal-counter { gap: 15px; }
}
@media (min-width: 401px) and (max-width: 420px) {
  .slider-countdown { bottom: 275px; left: 49%; max-width: 255px; transform: translateX(-49%); }
  .end-deal-counter div span { font-size: 28px; line-height: 30px; }
  .end-deal-counter, .end-deal-counter div { font-size: 14px; }
  .end-deal-counter { gap: 15px; }
}
@media (min-width: 381px) and (max-width: 400px) {
  .slider-countdown { bottom: 265px; left: 49%; max-width: 255px; transform: translateX(-49%); }
  .end-deal-counter div span { font-size: 28px; line-height: 30px; }
  .end-deal-counter, .end-deal-counter div { font-size: 14px; }
  .end-deal-counter { gap: 15px; }
}
@media (min-width: 371px) and  (max-width: 380px){
  .slider-countdown { bottom: 252px; left: 50%; max-width: 225px; transform: translateX(-50%); }
  .end-deal-counter div span { font-size: 28px; line-height: 30px; }
  .end-deal-counter, .end-deal-counter div { font-size: 14px; }
  .end-deal-counter { gap: 12px; }
}
@media (min-width: 361px) and (max-width: 370px) {
  .slider-countdown { bottom: 245px; max-width: 225px; }
  .end-deal-counter div span { font-size: 24px; line-height: 30px; }
  .end-deal-counter, .end-deal-counter div { font-size: 14px; }
  .end-deal-counter { gap: 12px; }
}
@media (max-width: 360px) {
  .slider-countdown { bottom: 235px; }
}
</style>
{%- assign item = section.settings -%}
{%- assign grid = section.settings.items -%}
{%- assign mobgrid = section.settings['mob-items'] -%}
<div
  id="section-{{ section.id }}"
  class="{% if item.top_offset == 'none' %}nomargin {% elsif item.top_offset == 'small' %}tt-offset-small {% endif %} custom-banner-row"
>
  <div class=" full-width-container">
    <div class="{%- if section.blocks.size > 1 -%} custom-{% endif %}slideshow  noarrow custom-slideshow-container">
      {%- for block in section.blocks -%}
        {%- assign item = block.settings -%}
        <div class="slider-itemslider">
          <a href="{{item.collection }}" aria-label="bannerlinks" rel="link">
            <div class="grid-column-itemslider">
              <div class="custom-banner-gd">
                {%- unless item.image == blank -%}
                  {%- if forloop.index == 1 -%}
                    {% comment %}  <link rel="preload" as="image" href="{{ item.image | img_url: ''}}">{% endcomment %}
                  {%- endif %}

                  <img
                    class="slidebanner{{ forloop.index }} slidebanner mobile-{%- unless  item.image == blank -%}hide{%- endunless -%}"
                    decoding="async"
                    srcset="{{ item.image | img_url: '1600x'}}"
                    id="{{ section.settings.tracking-id  | strip}}{{ forloop.index }}"
                    alt="banner image"
                    width="100%"
                    height="100%"
                    decoding="async"
                    {%- if forloop.index == 1 -%}
                      fetch-priority="high"
                      loading="eager"
                    {% else %}
                      fetch-priority="high"
                      loading="eager"
                    {%- endif %}
                    {% comment %}
                      srcset="{{ item.image | img_url: '1600x'}}"
                    {% endcomment %}
                    {%- if forloop.index == 1 -%}
                    {% endif %}
                  >
                {%- endunless -%}
                {%- unless item.image == blank -%}
                  {%- if forloop.index == 1 -%}
                    {% comment %}  <link rel="preload" as="image" href="{{ item.mob-image | img_url: '500x598'}}">{% endcomment %}
                  {%- endif %}

                  <img
                    class="slidebanner{{ forloop.index }} slidebanner desktop-{%- unless  item.image == blank -%}hide {%- endunless -%} "
                    decoding="async"
                    srcset="{{ item.mob-image | img_url: '450x'}}"
                    id="{{ section.settings.tracking-id  | strip}}{{ forloop.index }}"
                    alt="banner image"
                    width="100%"
                    height="100%"
                    loading="eager"
                    {%- if forloop.index == 1 -%}
                      fetch-priority="high"
                      loading="eager"
                    {% else %}
                      fetch-priority="high"
                      loading="eager"
                    {%- endif %}
                    {% comment %}
                      srcset="{{ item.mob-image | img_url: '450x'}}"
                    {% endcomment %}
                    {%- if forloop.index == 1 -%}
                    {% endif %}
                  >
                {%- endunless -%}
                  {%- if forloop.index == 1 -%}
                    <div class="slider-countdown">
                      <div class="slide-count">
                        <div class="end-deal-counter coun-timer-deal" id="countdown-{{ section.id }}"></div>
                      </div>
                    </div>
                  {%- endif -%}
              </div>
            </div>
          </a>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>
{% comment %}
  <script>
    // Select all images with the class 'lazyload'
  const lazyImages = document.querySelectorAll('img.lazyload');

  // Loop through the NodeList and remove the class
  lazyImages.forEach(img => {
    img.classList.remove('lazyload');
  });
  </script>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const lazyImages = document.querySelectorAll('img[loading="lazy"]');
      lazyImages.forEach(img => img.removeAttribute('loading'));
    });
  </script>
{% endcomment %}

<script defer>
  // Set the lightning deal start and end dates
  const lightningStartDate = new Date('{{ settings.startdate }}');
  const lightningEndDate = new Date('{{ settings.enddate }}');

  // Function to update the countdown timer
  function updateCountdown() {
    const currentDate = new Date();
    const timeRemaining = lightningEndDate - currentDate;

    if (timeRemaining > 0) {
      const totalHours = Math.floor(timeRemaining / (1000 * 60 * 60)); // Convert total ms to hours
      const minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);

      document.getElementById('countdown-{{section.id}}').innerHTML = 
        `<div><span>${totalHours}</span><br>Hours</div>:<div><span>${minutes}</span><br>Mins</div>:<div><span>${seconds}</span><br>Secs</div>`;
      
      document.getElementById('countdown-{{section.id}}').style.display = 'block';
    } else {
      document.getElementById('countdown-{{section.id}}').style.display = 'none';
      document.querySelector("#LDdeal-{{ section.id }}").classList.add("hide");
      // Optional: show sold out container if needed
      // document.querySelector("#soldallproduct-{{ section.id }}").classList.remove("hide");
    }
  }

  // Run updateCountdown every second
  setInterval(updateCountdown, 1000);
</script>

{% schema %}
{
  "name": "Custom Slidershow",
  "class": "index-section",
  "settings": [
      {
        "label": "Tracking id",
        "info": "Do Not Delete",
        "id": "tracking-id",
        "type": "text"
      }

  ],
  "blocks": [
  {
    "type": "item",
    "name": "Slider",
    "settings": [
      {
        "label": "Link",
        "id": "collection",
        "type": "url"
      },
      {
        "type": "image_picker",
        "id": "image",
        "label": "Custom Image"
      },
      {
        "type": "image_picker",
        "id": "mob-image",
        "label": "Custom Mpbile Image"
      }

    ]
  }
],
"presets": [
  {
    "name": "Custom slideshow",
    "category": "Slider",
    "blocks": [
    ]
  }
]
}
{% endschema %}
